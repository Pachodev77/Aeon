export class H2RVisualizer {
  private container: HTMLElement | null = null;

  constructor(container: HTMLElement) {
    this.container = container;
  }

  public start() {
    // No operation - visualization is empty
  }

  public stop() {
    // No operation - visualization is empty
  }

  public dispose() {
    // No operation - nothing to clean up
  }

  public handleResize() {
    // No operation - nothing to resize
  }
}
